import { style } from '@vanilla-extract/css';

export const container = style({
  display: 'grid',
  gap: '0.5rem',
  width: '100%',
});

export const chip = style({
  display: 'flex',
  gap: '0.5rem',
  width: '100%',
});
